<div class="notes">
  <ul class="left-sidebar">
    {{#each model as |note|}}
      {{#link-to "notes.show" note tagName="li"}}
        {{link-to note.title "notes.show" note}}
      {{/link-to}}
    {{/each}}
  </ul>

  <div class="main-content">
    {{outlet}}
  </div>
</div>
